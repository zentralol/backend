# Zentra Backend 环境变量配置
#
# 使用方法：
#   1. 复制此文件为 .env：  cp .env.example .env
#   2. 填入实际值（.env 已被 .gitignore 忽略，不会提交到仓库）
#   3. 启动服务：           node server.js
#
# 应用通过 dotenv 在启动时自动加载 .env 文件。

# ---------------------------------------------------------------------------
# 服务端口
# ---------------------------------------------------------------------------
# HTTP 服务监听端口。未设置时默认为 3000。
PORT=3000

# ---------------------------------------------------------------------------
# PostgreSQL 数据库连接
# ---------------------------------------------------------------------------
# PostgreSQL 连接字符串（必填）。
# 格式：postgresql://<用户名>:<密码>@<主机>:<端口>/<数据库名>
#
# 本地示例：
#   DATABASE_URL=postgresql://postgres:password@localhost:5432/zentra
#
# 云数据库（如 Supabase、Neon、Railway）通常提供完整的连接 URL，
# 并可能需要 SSL（server.js 已启用 ssl: { rejectUnauthorized: false }）。
DATABASE_URL=postgresql://user:password@localhost:5432/zentra
